{
  "rules": {
    "domains": {
      ".read": true,
      "$id": {
        "likes": {
          ".write": "newData.exists()",
          ".validate": "newData.isNumber()"
        }
      }
    }
  }
}
